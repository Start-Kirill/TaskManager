spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://db:5432/user_service?ApplicationName=user-service
    username: user_service
    password: q1w2e3r4
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate.default_schema: app
  mail:
    host: smtp.gmail.com
    port: 587
    username: testovich061@gmail.com
    password: dfzmovdbupqiwccb
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
logging:
  level:
    org.springframework.web: DEBUG
server:
  port: 8080
app:
  audit:
    url: http://audit-service:${server.port}/audit
  verification:
    url: http://user-service:${server.port}/users/verification

